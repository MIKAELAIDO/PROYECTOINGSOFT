| 1. Id de prueba | 2. Nombre de la integración | 3. Módulo de origen (pantalla inicial) | 4. Módulo destino (pantalla final) | 5. Resultado esperado | 6. Estado Actual |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **INT-01** | Registro de nuevo usuario (`formRegistro`) | Vista de Autenticación (Formulario de Registro) | Vista de Autenticación (Formulario de Login) | Al ingresar datos válidos (contraseñas coincidentes >= 6 caracteres y matrícula no duplicada), el usuario se almacena en `localStorage` y la app redirige al formulario de Login mostrando un mensaje de éxito. | **PASA** |
| **INT-02** | Inicio de sesión (`formLogin`) | Vista de Autenticación (Formulario de Login) | Aplicación Principal (`vistaApp` / Panel Buscar) | Al ingresar matrícula y contraseña correctas, se crea el objeto de sesión en `localStorage`, se oculta la pantalla de autenticación y se muestra la interfaz principal con el nombre del usuario en el header. | **PASA** |
| **INT-03** | Cierre de sesión (`btnCerrarSesion`) | Aplicación Principal (`vistaApp`) | Vista de Autenticación (Formulario de Login) | Al hacer clic en "Cerrar sesión", se remueve la sesión del `localStorage`, se ocultan los paneles de la app y se redirige forzadamente al formulario de Login. | **PASA** |
| **INT-04** | Publicación de objeto (`formPublicar`) | Panel "Publicar un objeto" (`panelPublicar`) | Panel "Buscar objetos" (`panelBuscar`) | Tras rellenar los datos del objeto y las 2 preguntas de seguridad, el formulario se reinicia, los datos se guardan en el arreglo global y el nuevo objeto aparece renderizado de inmediato en el tablón. | **PASA** |
| **INT-05** | Búsqueda y Filtrado en Tiempo Real | Panel "Buscar objetos" (Filtros interactivos) | Panel "Buscar objetos" (Lista de tarjetas actualizada) | Al escribir palabras clave o cambiar los selectores (Tipo, Categoría, Ubicación), la función `renderizarObjetos()` filtra de forma dinámica los elementos del DOM sin necesidad de recargar la página. | **PASA** |
| **INT-06** | Validación y Reclamo de Objeto (`formReclamo`) | Panel "Buscar objetos" (Modal de Reclamo) | Panel "Buscar objetos" (Tablón actualizado) | Al pulsar "Reclamar", se abre un modal con preguntas dinámicas. Si las respuestas coinciden con las guardadas, el estado del objeto cambia a `reclamado: true`, se cierra el modal y el objeto desaparece del tablón público. | **PASA** |
